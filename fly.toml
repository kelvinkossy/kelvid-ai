app = "kelvid-ai"
primary_region = "lhr"

[env]
  FLY_VOLUME_PATH = "/data"
  NEXT_PUBLIC_BASE_URL = "https://kelvid-ai.fly.dev"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0

[[mounts]]
  source = "kelvid_data"
  destination = "/data"
